{
  "name": "pharma-client",
  "version": "1.0.0",
  "scripts": {
    "dev": "node ./node_modules/vite/bin/vite.js",
    "build": "tsc -b && vite build && react-scripts build",
    "preview": "vite preview"
  }
}